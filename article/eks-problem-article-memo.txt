
NodeGroup を削除 → 節約

% eksctl delete nodegroup \
  --cluster webgame-cluster \
  --region us-east-1 \
  --name spot-ng
	
	
AWS Console → EC2
Auto Scaling Groups
名前が以下のようなものを探す：
eks-webgame-cluster-spot-ng-xxxx
Delete

その後
EC2 は Terminate
Node は消える
eksctl の drain 待ちは無意味になる

% kubectl get nodes
No resources found

